#include <QSettings>

#ifndef CONFIG_H
#define CONFIG_H

class Config
{
    /*
public:
    Config();
    bool writeImpl(QIODevice &device, const QSettings::SettingsMap &map);
    bool readImpl(QIODevice &device, QSettings::SettingsMap &map);
*/
};

#endif // CONFIG_H
